# Feature: db

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | feature (2) |
| Scope | db |
| Active | 2026-04-20 to 2026-05-07 |
| Commits | 2 |
| People | Priya Nair (1), Mina Kowalski (1) |
| Ask first | Priya Nair <priya@example.com> |
| Files | 6 |
| Functions | 15 |
| Highest risk | query - 29 (medium) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-04-20 **feature**: postgres schema and repositories - Mina Kowalski
- 2026-05-07 **feature**: sessions and tags tables - Priya Nair

## Where the code lives

### Files

- `db/migrations/001_users_posts.sql` - 1 commit
- `db/migrations/003_sessions_tags.sql` - 1 commit
- `src/config.js` - 1 commit
- `src/db/pool.js` - 1 commit
- `src/db/postsRepo.js` - 1 commit
- `src/tags.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `query` | src/db/pool.js:9-17 | 29 (medium) | 12 | 1 | no data |
| `normalizeTag` | src/tags.js:3-5 | 7 (low) | 1 | 1 | no data |
| `mergeTags` | src/tags.js:7-9 | 7 (low) | 1 | 1 | no data |
| `popularTags` | src/tags.js:11-22 | 7 (low) | 1 | 0 | no data |
| `requireEnv` | src/config.js:2-8 | 7 (low) | 1 | 0 | no data |
| `envFlag` | src/config.js:10-16 | 7 (low) | 1 | 0 | no data |
| `envNumber` | src/config.js:18-21 | 7 (low) | 1 | 0 | no data |
| `loadConfig` | src/config.js:23-33 | 7 (low) | 1 | 3 | no data |
| `withTransaction` | src/db/pool.js:19-29 | 7 (low) | 1 | 1 | no data |
| `saveTags` | src/db/postsRepo.js:31-36 | 7 (low) | 1 | 0 | no data |
| `createPool` | src/db/pool.js:4-7 | -4 (unused) | 0 | 1 | no data |
| `findPostById` | src/db/postsRepo.js:5-8 | -4 (unused) | 0 | 1 | no data |
| `listPublished` | src/db/postsRepo.js:10-17 | -8 (unused) | 0 | 1 | no data |
| `updatePost` | src/db/postsRepo.js:38-46 | -9 (unused) | 0 | 2 | no data |
| `insertPost` | src/db/postsRepo.js:19-29 | -11 (unused) | 0 | 4 | no data |

## How it fits together

### Calls inside the feature

- `mergeTags` -> `normalizeTag`
- `loadConfig` -> `envNumber`
- `loadConfig` -> `requireEnv`
- `loadConfig` -> `envFlag`
- `withTransaction` -> `query`
- `findPostById` -> `query`
- `listPublished` -> `query`
- `updatePost` -> `query`
- `insertPost` -> `withTransaction`
- `insertPost` -> `saveTags`
- `insertPost` -> `mergeTags`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `query` is called by `handleSearch` (src/api.js:54-58), `find` (src/app.js:33-35), `rankResults` (src/search.js:32-38), `search` (src/search.js:40-43), `searchPosts` (web/src/api/client.ts:62-64), `PostList` (web/src/components/PostList.tsx:5-25), `useSearch` (web/src/hooks/usePosts.ts:18-24), `queryFn` (web/src/hooks/usePosts.ts:21)
- `popularTags` is called by `dailyReport` (src/admin/reports.js:14-23)
- `loadConfig` is called by `start` (src/server.js:20-31)

### What the feature depends on

- `trace` (src/telemetry.js:3-9) - used by `query`, `createPool`
- `slugify` (src/format.js:1-3) - used by `normalizeTag`
- `recordChange` (src/audit.js:3-7) - used by `updatePost`, `insertPost`

## Risks to keep in mind

- `query` - MEDIUM (29): 12 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `normalizeTag` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `mergeTags` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `popularTags` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `requireEnv` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `envFlag` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `envNumber` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `loadConfig` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `withTransaction` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `saveTags` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `createPool` - UNUSED (-4): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `findPostById` - UNUSED (-4): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `listPublished` - UNUSED (-8): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `updatePost` - UNUSED (-9): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `insertPost` - UNUSED (-11): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1

## Source

### query - src/db/pool.js:9-17

```javascript
async function query(pool, sql, params) {
  trace('db.query', { sql: sql.slice(0, 40) });
  pool.active++;
  try {
    return await pool.driver.query(sql, params || []);
  } finally {
    pool.active--;
  }
}
```

### normalizeTag - src/tags.js:3-5

```javascript
function normalizeTag(tag) {
  return slugify(String(tag)).slice(0, 32);
}
```

### mergeTags - src/tags.js:7-9

```javascript
function mergeTags(existing, incoming) {
  return [...new Set([...(existing || []), ...(incoming || [])].map(normalizeTag).filter(Boolean))];
}
```

### popularTags - src/tags.js:11-22

```javascript
function popularTags(posts, limit) {
  const counts = new Map();
  for (const post of posts) {
    for (const tag of post.tags || []) {
      counts.set(tag, (counts.get(tag) || 0) + 1);
    }
  }
  return [...counts.entries()]
    .sort((a, b) => b[1] - a[1])
    .slice(0, limit || 10)
    .map(([tag, count]) => ({ tag, count }));
}
```

### requireEnv - src/config.js:2-8

```javascript
function requireEnv(name) {
  const value = process.env[name];
  if (!value) {
    throw new Error(`missing required env var ${name}`);
  }
  return value;
}
```

### envFlag - src/config.js:10-16

```javascript
function envFlag(name, fallback) {
  const value = process.env[name];
  if (value === undefined) {
    return fallback;
  }
  return value === '1' || value.toLowerCase() === 'true';
}
```

### envNumber - src/config.js:18-21

```javascript
function envNumber(name, fallback) {
  const value = Number(process.env[name]);
  return Number.isFinite(value) ? value : fallback;
}
```

### loadConfig - src/config.js:23-33

```javascript
function loadConfig() {
  return {
    port: envNumber('PORT', 3000),
    databaseUrl: requireEnv('DATABASE_URL'),
    redisUrl: process.env.REDIS_URL || 'redis://localhost:6379',
    mongoUrl: process.env.MONGO_URL || 'mongodb://localhost:27017/inkwell',
    sessionTtlHours: envNumber('SESSION_TTL_HOURS', 72),
    rateLimitPerMinute: envNumber('RATE_LIMIT_PER_MINUTE', 120),
    telemetry: envFlag('TELEMETRY', true),
  };
}
```

### withTransaction - src/db/pool.js:19-29

```javascript
async function withTransaction(pool, work) {
  await query(pool, 'BEGIN');
  try {
    const result = await work((sql, params) => query(pool, sql, params));
    await query(pool, 'COMMIT');
    return result;
  } catch (err) {
    await query(pool, 'ROLLBACK');
    throw err;
  }
}
```

### saveTags - src/db/postsRepo.js:31-36

```javascript
async function saveTags(run, postId, tags) {
  for (const tag of tags) {
    await run('INSERT INTO tags (name) VALUES ($1) ON CONFLICT DO NOTHING', [tag]);
    await run('INSERT INTO post_tags (post_id, tag_name) VALUES ($1, $2)', [postId, tag]);
  }
}
```

### createPool - src/db/pool.js:4-7

```javascript
function createPool(databaseUrl, driver) {
  trace('db.pool.create', {});
  return { url: databaseUrl, driver, active: 0 };
}
```

### findPostById - src/db/postsRepo.js:5-8

```javascript
async function findPostById(pool, id) {
  const { rows } = await query(pool, 'SELECT * FROM posts WHERE id = $1', [id]);
  return rows[0] || null;
}
```

### listPublished - src/db/postsRepo.js:10-17

```javascript
async function listPublished(pool, limit, offset) {
  const { rows } = await query(
    pool,
    'SELECT id, title, slug, published_at FROM posts WHERE status = $1 ORDER BY published_at DESC LIMIT $2 OFFSET $3',
    ['published', limit || 20, offset || 0],
  );
  return rows;
}
```

### updatePost - src/db/postsRepo.js:38-46

```javascript
async function updatePost(pool, id, changes, editorId) {
  recordChange(editorId, 'update', `posts/${id}`);
  const { rows } = await query(
    pool,
    'UPDATE posts SET title = COALESCE($2, title), body = COALESCE($3, body), updated_at = now() WHERE id = $1 RETURNING *',
    [id, changes.title, changes.body],
  );
  return rows[0] || null;
}
```

### insertPost - src/db/postsRepo.js:19-29

```javascript
async function insertPost(pool, post, authorId) {
  recordChange(authorId, 'insert', 'posts');
  return withTransaction(pool, async (run) => {
    const { rows } = await run(
      'INSERT INTO posts (author_id, title, slug, body, status) VALUES ($1, $2, $3, $4, $5) RETURNING id',
      [authorId, post.title, post.slug, post.body, 'draft'],
    );
    await saveTags(run, rows[0].id, mergeTags([], post.tags));
    return rows[0].id;
  });
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 056df433 | 2026-05-07 | Priya Nair | feature | sessions and tags tables | 2 |
| 3f1ebe04 | 2026-04-20 | Mina Kowalski | feature | postgres schema and repositories | 4 |
