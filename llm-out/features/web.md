# Feature: web

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | feature (3), style (1), bug fix (1) |
| Scope | web |
| Active | 2026-06-26 to 2026-09-04 |
| Commits | 5 |
| People | Leo Park (5) |
| Ask first | Leo Park <leo@example.com> |
| Files | 13 |
| Functions | 39 |
| Highest risk | request - 36 (high) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-06-26 **feature**: react frontend with post list - Leo Park
- 2026-06-30 **feature**: comment thread and sign in - Leo Park
- 2026-08-01 **bug fix**: search only after two characters - Leo Park
- 2026-08-20 **style**: round reading time up - Leo Park
- 2026-09-04 **feature**: reading time on cards - Leo Park

## Where the code lives

### Files

- `web/src/lib/time.ts` - 3 commits
- `web/src/hooks/usePosts.ts` - 2 commits
- `web/package.json` - 1 commit
- `web/src/api/client.ts` - 1 commit
- `web/src/App.tsx` - 1 commit
- `web/src/components/CommentThread.tsx` - 1 commit
- `web/src/components/LegacyBanner.tsx` - 1 commit
- `web/src/components/Navbar.tsx` - 1 commit
- `web/src/components/PostCard.tsx` - 1 commit
- `web/src/components/PostList.tsx` - 1 commit
- `web/src/components/TagList.tsx` - 1 commit
- `web/src/lib/time.test.ts` - 1 commit
- `web/src/main.tsx` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `request` | web/src/api/client.ts:26-36 | 36 (high) | 15 | 0 | no data |
| `comments` | web/src/hooks/usePosts.ts:6 | 18 (medium) | 6 | 0 | no data |
| `readingTime` | web/src/lib/time.ts:13-16 | 10 (low) | 1 | 0 | no data |
| `all` | web/src/hooks/usePosts.ts:5 | 10 (low) | 2 | 0 | no data |
| `timeAgo` | web/src/lib/time.ts:5-11 | 10 (low) | 2 | 0 | no data |
| `useSearch` | web/src/hooks/usePosts.ts:18-24 | 9 (low) | 1 | 2 | no data |
| `CommentItem` | web/src/components/CommentThread.tsx:6-22 | 8 (low) | 1 | 1 | no data |
| `CommentThread` | web/src/components/CommentThread.tsx:24-50 | 8 (low) | 1 | 4 | no data |
| `submit` | web/src/components/CommentThread.tsx:30-35 | 8 (low) | 1 | 0 | no data |
| `Navbar` | web/src/components/Navbar.tsx:4-29 | 8 (low) | 1 | 1 | no data |
| `handleLogin` | web/src/components/Navbar.tsx:9-13 | 8 (low) | 1 | 2 | no data |
| `goHome` | web/src/App.tsx:9 | 8 (low) | 1 | 0 | no data |
| `getPosts` | web/src/api/client.ts:38-40 | 8 (low) | 1 | 1 | no data |
| `createPost` | web/src/api/client.ts:46-48 | 8 (low) | 1 | 1 | no data |
| `getComments` | web/src/api/client.ts:50-52 | 8 (low) | 1 | 1 | no data |
| `login` | web/src/api/client.ts:58-60 | 8 (low) | 1 | 1 | no data |
| `searchPosts` | web/src/api/client.ts:62-64 | 8 (low) | 1 | 2 | no data |
| `PostCard` | web/src/components/PostCard.tsx:10-22 | 8 (low) | 1 | 3 | no data |
| `PostList` | web/src/components/PostList.tsx:5-25 | 8 (low) | 1 | 5 | no data |
| `handleQuery` | web/src/components/PostList.tsx:10 | 8 (low) | 1 | 0 | no data |
| `TagList` | web/src/components/TagList.tsx:1-12 | 8 (low) | 1 | 0 | no data |
| `search` | web/src/hooks/usePosts.ts:7 | 8 (low) | 1 | 0 | no data |
| `usePosts` | web/src/hooks/usePosts.ts:10-12 | 8 (low) | 1 | 2 | no data |
| `useComments` | web/src/hooks/usePosts.ts:14-16 | 8 (low) | 1 | 1 | no data |
| `useAddComment` | web/src/hooks/usePosts.ts:34-40 | 8 (low) | 1 | 0 | no data |
| `App` | web/src/App.tsx:6-17 | 6 (low) | 0 | 5 | no data |
| `constructor` | web/src/api/client.ts:21-23 | 6 (low) | 0 | 0 | no data |
| `getPost` | web/src/api/client.ts:42-44 | 6 (low) | 0 | 1 | no data |
| `addComment` | web/src/api/client.ts:54-56 | 6 (low) | 0 | 1 | no data |
| `queryFn` | web/src/hooks/usePosts.ts:15 | 6 (low) | 0 | 1 | no data |
| `queryFn` | web/src/hooks/usePosts.ts:21 | 6 (low) | 0 | 2 | no data |
| `onSuccess` | web/src/hooks/usePosts.ts:30 | 6 (low) | 0 | 1 | no data |
| `mutationFn` | web/src/hooks/usePosts.ts:37 | 6 (low) | 0 | 1 | no data |
| `onSuccess` | web/src/hooks/usePosts.ts:38 | 6 (low) | 0 | 1 | no data |
| `queryFn` | web/src/hooks/usePosts.ts:46 | 6 (low) | 0 | 0 | no data |
| `formatDate` | web/src/lib/time.ts:18-20 | -3 (unused) | 0 | 0 | no data |
| `useLocalDrafts` | web/src/hooks/usePosts.ts:43-48 | -6 (unused) | 0 | 0 | no data |
| `LegacyBanner` | web/src/components/LegacyBanner.tsx:2-8 | -7 (unused) | 0 | 0 | no data |
| `useCreatePost` | web/src/hooks/usePosts.ts:26-32 | -7 (unused) | 0 | 1 | no data |

## How it fits together

### Calls inside the feature

- `useSearch` -> `search`
- `CommentItem` -> `timeAgo`
- `CommentThread` -> `useComments`
- `CommentThread` -> `useAddComment`
- `CommentThread` -> `CommentItem`
- `CommentThread` -> `submit`
- `Navbar` -> `handleLogin`
- `handleLogin` -> `login`
- `getPosts` -> `request`
- `createPost` -> `request`
- `getComments` -> `request`
- `login` -> `request`
- `searchPosts` -> `request`
- `PostCard` -> `readingTime`
- `PostCard` -> `timeAgo`
- `PostCard` -> `TagList`
- `PostList` -> `usePosts`
- `PostList` -> `useSearch`
- `PostList` -> `handleQuery`
- `PostList` -> `PostCard`
- `usePosts` -> `all`
- `usePosts` -> `getPosts`
- `useComments` -> `comments`
- `App` -> `Navbar`
- `App` -> `goHome`
- `App` -> `PostList`
- `App` -> `CommentThread`
- `getPost` -> `request`
- `addComment` -> `request`
- `queryFn` -> `getComments`
- `queryFn` -> `searchPosts`
- `onSuccess` -> `all`
- `onSuccess` -> `comments`
- `useCreatePost` -> `createPost`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `request` is called by `handleCreate` (src/api.js:30-39), `handleDelete` (src/api.js:41-52), `handleAddComment` (src/api.js:78-85), `handleModerate` (src/api.js:87-94), `handleReport` (src/api.js:116-123), `requireUser` (src/auth/middleware.js:17-27), `requireRole` (src/auth/middleware.js:29-38), `start` (src/server.js:20-31)
- `comments` is called by `handleComments` (src/api.js:73-76), `handleAddComment` (src/api.js:78-85), `handleModerate` (src/api.js:87-94), `formatDigestLine` (src/notifications.js:24-26)

### What the feature depends on

- `query` (src/db/pool.js:9-17) - used by `useSearch`, `searchPosts`, `PostList`, `queryFn`
- `ok` (src/api.js:125-127) - used by `handleLogin`
- `main` (services/mailer/main.go:15-26) - used by `App`
- `addComment` (src/comments.js:18-37) - used by `mutationFn`

## Risks to keep in mind

- `request` - HIGH (36): 15 callers, coverage no data, 1 change in 90 days, bus factor 1
- `comments` - MEDIUM (18): 6 callers, coverage no data, 1 change in 90 days, bus factor 1
- `readingTime` - LOW (10): 1 caller, coverage no data, 3 changes in 90 days, bus factor 1
- `all` - LOW (10): 2 callers, coverage no data, 1 change in 90 days, bus factor 1
- `timeAgo` - LOW (10): 2 callers, coverage no data, 1 change in 90 days, bus factor 1
- `useSearch` - LOW (9): 1 caller, coverage no data, 2 changes in 90 days, bus factor 1
- `CommentItem` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `CommentThread` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `submit` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `Navbar` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `handleLogin` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `goHome` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `getPosts` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `createPost` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `getComments` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `login` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `searchPosts` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `PostCard` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `PostList` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `handleQuery` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `TagList` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `search` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `usePosts` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `useComments` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `useAddComment` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `App` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `constructor` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `getPost` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `addComment` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `queryFn` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `queryFn` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `onSuccess` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `mutationFn` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `onSuccess` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `queryFn` - LOW (6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `formatDate` - UNUSED (-3): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `useLocalDrafts` - UNUSED (-6): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `LegacyBanner` - UNUSED (-7): 0 callers, coverage no data, 1 change in 90 days, bus factor 1
- `useCreatePost` - UNUSED (-7): 0 callers, coverage no data, 1 change in 90 days, bus factor 1

## Source

### request - web/src/api/client.ts:26-36

```typescript
export async function request<T>(path: string, init?: RequestInit): Promise<T> {
  const response = await fetch(`${BASE}${path}`, {
    credentials: "include",
    headers: { "content-type": "application/json" },
    ...init,
  });
  if (!response.ok) {
    throw new ApiError(response.status, await response.text());
  }
  return response.json() as Promise<T>;
}
```

### comments - web/src/hooks/usePosts.ts:6

```typescript
  comments: (postId: number) => ["posts", postId, "comments"] as const,
```

### readingTime - web/src/lib/time.ts:13-16

```typescript
export function readingTime(text: string): string {
  const words = text.trim().split(/\s+/).filter(Boolean).length;
  return `${Math.max(1, Math.round(words / 220))} min read`;
}
```

### all - web/src/hooks/usePosts.ts:5

```typescript
  all: () => ["posts"] as const,
```

### timeAgo - web/src/lib/time.ts:5-11

```typescript
export function timeAgo(timestamp: number, now = Date.now()): string {
  const diff = Math.max(0, now - timestamp);
  if (diff < MINUTE) return "just now";
  if (diff < HOUR) return `${Math.floor(diff / MINUTE)}m ago`;
  if (diff < DAY) return `${Math.floor(diff / HOUR)}h ago`;
  return `${Math.floor(diff / DAY)}d ago`;
}
```

### useSearch - web/src/hooks/usePosts.ts:18-24

```typescript
export function useSearch(query: string) {
  return useQuery({
    queryKey: postKeys.search(query),
    queryFn: () => searchPosts(query),
    enabled: query.trim().length > 1,
  });
}
```

### CommentItem - web/src/components/CommentThread.tsx:6-22

```tsx
function CommentItem({ comment, onReply }: { comment: Comment; onReply: (id: number) => void }) {
  return (
    <li>
      <p>{comment.body}</p>
      <small>
        {timeAgo(comment.createdAt)} · <button onClick={() => onReply(comment.id)}>reply</button>
      </small>
      {comment.replies.length > 0 && (
        <ul>
          {comment.replies.map((reply) => (
            <CommentItem key={reply.id} comment={reply} onReply={onReply} />
          ))}
        </ul>
      )}
    </li>
  );
}
```

### CommentThread - web/src/components/CommentThread.tsx:24-50

```tsx
export function CommentThread({ postId }: { postId: number }) {
  const comments = useComments(postId);
  const add = useAddComment(postId);
  const [text, setText] = useState("");
  const [parentId, setParentId] = useState<number | undefined>();

  const submit = (event: React.FormEvent) => {
    event.preventDefault();
    add.mutate({ text, parentId });
    setText("");
    setParentId(undefined);
  };

  return (
    <section className="comments">
      <ul>
        {(comments.data ?? []).map((comment) => (
          <CommentItem key={comment.id} comment={comment} onReply={setParentId} />
        ))}
      </ul>
      <form onSubmit={submit}>
        <textarea value={text} onChange={(e) => setText(e.target.value)} />
        <button disabled={!text.trim() || add.isPending}>{parentId ? "Reply" : "Comment"}</button>
      </form>
    </section>
  );
}
```

### submit - web/src/components/CommentThread.tsx:30-35

```tsx
  const submit = (event: React.FormEvent) => {
    event.preventDefault();
    add.mutate({ text, parentId });
    setText("");
    setParentId(undefined);
  };
```

### Navbar - web/src/components/Navbar.tsx:4-29

```tsx
export function Navbar({ onHome }: { onHome: () => void }) {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [signedIn, setSignedIn] = useState(false);

  const handleLogin = async (event: React.FormEvent) => {
    event.preventDefault();
    const result = await login(email, password);
    setSignedIn(result.ok);
  };

  return (
    <nav>
      <button onClick={onHome}>Inkwell</button>
      {signedIn ? (
        <span>Signed in</span>
      ) : (
        <form onSubmit={handleLogin}>
          <input value={email} onChange={(e) => setEmail(e.target.value)} placeholder="email" />
          <input value={password} onChange={(e) => setPassword(e.target.value)} type="password" />
          <button>Sign in</button>
        </form>
      )}
    </nav>
  );
}
```

### handleLogin - web/src/components/Navbar.tsx:9-13

```tsx
  const handleLogin = async (event: React.FormEvent) => {
    event.preventDefault();
    const result = await login(email, password);
    setSignedIn(result.ok);
  };
```

### goHome - web/src/App.tsx:9

```tsx
  const goHome = () => setOpenPost(null);
```

### getPosts - web/src/api/client.ts:38-40

```typescript
export function getPosts(): Promise<{ count: number; cards: Post[] }> {
  return request("/posts");
}
```

### createPost - web/src/api/client.ts:46-48

```typescript
export function createPost(input: { title: string; body: string; tags: string[] }): Promise<Post> {
  return request("/posts", { method: "POST", body: JSON.stringify(input) });
}
```

### getComments - web/src/api/client.ts:50-52

```typescript
export function getComments(postId: number): Promise<Comment[]> {
  return request(`/posts/${postId}/comments`);
}
```

### login - web/src/api/client.ts:58-60

```typescript
export function login(email: string, password: string): Promise<{ ok: boolean }> {
  return request("/login", { method: "POST", body: JSON.stringify({ email, password }) });
}
```

### searchPosts - web/src/api/client.ts:62-64

```typescript
export function searchPosts(query: string): Promise<{ post: Post; score: number }[]> {
  return request(`/search?q=${encodeURIComponent(query)}`);
}
```

### PostCard - web/src/components/PostCard.tsx:10-22

```tsx
export function PostCard({ post, onOpen }: PostCardProps) {
  return (
    <article className="post-card" onClick={() => onOpen(post.id)}>
      <h2>{post.heading}</h2>
      <p>{post.blurb}</p>
      <footer>
        <span>{readingTime(post.blurb)}</span>
        {post.createdAt ? <time>{timeAgo(post.createdAt)}</time> : null}
        <TagList tags={post.tags ?? []} />
      </footer>
    </article>
  );
}
```

### PostList - web/src/components/PostList.tsx:5-25

```tsx
export function PostList({ onOpen }: { onOpen: (id: number) => void }) {
  const [query, setQuery] = useState("");
  const posts = usePosts();
  const results = useSearch(query);

  const handleQuery = (event: React.ChangeEvent<HTMLInputElement>) => setQuery(event.target.value);

  if (posts.isLoading) {
    return <p>Loading…</p>;
  }
  const cards = query.trim().length > 1 ? (results.data ?? []).map((hit) => hit.post) : posts.data?.cards ?? [];

  return (
    <section>
      <input placeholder="Search posts" value={query} onChange={handleQuery} />
      {cards.map((post) => (
        <PostCard key={post.id} post={post} onOpen={onOpen} />
      ))}
    </section>
  );
}
```

### handleQuery - web/src/components/PostList.tsx:10

```tsx
  const handleQuery = (event: React.ChangeEvent<HTMLInputElement>) => setQuery(event.target.value);
```

### TagList - web/src/components/TagList.tsx:1-12

```tsx
export function TagList({ tags }: { tags: string[] }) {
  if (!tags.length) {
    return null;
  }
  return (
    <ul className="tags">
      {tags.map((tag) => (
        <li key={tag}>#{tag}</li>
      ))}
    </ul>
  );
}
```

### search - web/src/hooks/usePosts.ts:7

```typescript
  search: (query: string) => ["posts", "search", query] as const,
```

### usePosts - web/src/hooks/usePosts.ts:10-12

```typescript
export function usePosts() {
  return useQuery({ queryKey: postKeys.all(), queryFn: getPosts });
}
```

### useComments - web/src/hooks/usePosts.ts:14-16

```typescript
export function useComments(postId: number) {
  return useQuery({ queryKey: postKeys.comments(postId), queryFn: () => getComments(postId) });
}
```

### useAddComment - web/src/hooks/usePosts.ts:34-40

```typescript
export function useAddComment(postId: number) {
  const client = useQueryClient();
  return useMutation({
    mutationFn: (input: { text: string; parentId?: number }) => addComment(postId, input.text, input.parentId),
    onSuccess: () => client.invalidateQueries({ queryKey: postKeys.comments(postId) }),
  });
}
```

### App - web/src/App.tsx:6-17

```tsx
export default function App() {
  const [openPost, setOpenPost] = useState<number | null>(null);

  const goHome = () => setOpenPost(null);

  return (
    <main>
      <Navbar onHome={goHome} />
      {openPost === null ? <PostList onOpen={setOpenPost} /> : <CommentThread postId={openPost} />}
    </main>
  );
}
```

### constructor - web/src/api/client.ts:21-23

```typescript
  constructor(public status: number, message: string) {
    super(message);
  }
```

### getPost - web/src/api/client.ts:42-44

```typescript
export function getPost(id: number): Promise<Post> {
  return request(`/posts/${id}`);
}
```

### addComment - web/src/api/client.ts:54-56

```typescript
export function addComment(postId: number, text: string, parentId?: number): Promise<Comment> {
  return request(`/posts/${postId}/comments`, { method: "POST", body: JSON.stringify({ text, parentId }) });
}
```

### queryFn - web/src/hooks/usePosts.ts:15

```typescript
  return useQuery({ queryKey: postKeys.comments(postId), queryFn: () => getComments(postId) });
```

### queryFn - web/src/hooks/usePosts.ts:21

```typescript
    queryFn: () => searchPosts(query),
```

### onSuccess - web/src/hooks/usePosts.ts:30

```typescript
    onSuccess: () => client.invalidateQueries({ queryKey: postKeys.all() }),
```

### mutationFn - web/src/hooks/usePosts.ts:37

```typescript
    mutationFn: (input: { text: string; parentId?: number }) => addComment(postId, input.text, input.parentId),
```

### onSuccess - web/src/hooks/usePosts.ts:38

```typescript
    onSuccess: () => client.invalidateQueries({ queryKey: postKeys.comments(postId) }),
```

### queryFn - web/src/hooks/usePosts.ts:46

```typescript
    queryFn: () => JSON.parse(localStorage.getItem("drafts") ?? "[]"),
```

### formatDate - web/src/lib/time.ts:18-20

```typescript
export function formatDate(timestamp: number): string {
  return new Date(timestamp).toLocaleDateString("en", { month: "short", day: "numeric", year: "numeric" });
}
```

### useLocalDrafts - web/src/hooks/usePosts.ts:43-48

```typescript
export function useLocalDrafts() {
  return useQuery({
    queryKey: ["drafts"],
    queryFn: () => JSON.parse(localStorage.getItem("drafts") ?? "[]"),
  });
}
```

### LegacyBanner - web/src/components/LegacyBanner.tsx:2-8

```tsx
export function LegacyBanner({ message }: { message: string }) {
  return (
    <div className="legacy-banner" role="status">
      <strong>Heads up:</strong> {message}
    </div>
  );
}
```

### useCreatePost - web/src/hooks/usePosts.ts:26-32

```typescript
export function useCreatePost() {
  const client = useQueryClient();
  return useMutation({
    mutationFn: createPost,
    onSuccess: () => client.invalidateQueries({ queryKey: postKeys.all() }),
  });
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| ea86d819 | 2026-09-04 | Leo Park | feature | reading time on cards | 1 |
| 8b95835d | 2026-08-20 | Leo Park | style | round reading time up | 1 |
| ccbc7d5e | 2026-08-01 | Leo Park | bug fix | search only after two characters | 1 |
| 86660a75 | 2026-06-30 | Leo Park | feature | comment thread and sign in | 2 |
| c00506ad | 2026-06-26 | Leo Park | feature | react frontend with post list | 11 |
