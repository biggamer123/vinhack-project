# Feature: api

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | refactor (1) |
| Scope | api |
| Active | 2026-09-11 to 2026-09-11 |
| Commits | 1 |
| People | Ada Reyes (1) |
| Ask first | Ada Reyes <ada@example.com> |
| Files | 1 |
| Functions | 1 |
| Highest risk | route - 12 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-09-11 **refactor**: single not-found path - Ada Reyes

## Where the code lives

### Files

- `src/api.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `route` | src/api.js:138-183 | 12 (low) | 3 | 16 | no data |

## How it fits together

### Calls inside the feature

- None - the feature's functions do not call one another.

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `route` is called by `newServer` (src/api.js:185-190), `handle` (src/app.js:29-31), `start` (src/server.js:20-31)

### What the feature depends on

- `handleList` (src/api.js:24-28) - used by `route`
- `handleRss` (src/api.js:112-114) - used by `route`
- `handleSearch` (src/api.js:54-58) - used by `route`
- `handleComments` (src/api.js:73-76) - used by `route`
- `idFrom` (src/api.js:134-136) - used by `route`
- `handleAddComment` (src/api.js:78-85) - used by `route`
- `handleModerate` (src/api.js:87-94) - used by `route`
- `handleSlug` (src/api.js:66-71) - used by `route`
- `handleGet` (src/api.js:14-22) - used by `route`
- `handleCreate` (src/api.js:30-39) - used by `route`
- `handleDelete` (src/api.js:41-52) - used by `route`
- `handleLogin` (src/api.js:96-105) - used by `route`
- `handleLogout` (src/api.js:107-110) - used by `route`
- `handleReport` (src/api.js:116-123) - used by `route`
- `handleExport` (src/api.js:60-64) - used by `route`
- `notFound` (src/api.js:129-132) - used by `route`

## Risks to keep in mind

- `route` - LOW (12): 3 callers, coverage no data, 1 change in 90 days, bus factor 1

## Source

### route - src/api.js:138-183

```javascript
function route(store, method, path, body, request) {
  const req = request || { headers: {} };
  if (method === 'GET' && path === '/posts') {
    return handleList(store);
  }
  if (method === 'GET' && path === '/feed.xml') {
    return handleRss(store);
  }
  if (method === 'GET' && path.startsWith('/search?q=')) {
    return handleSearch(store, decodeURIComponent(path.slice(10)));
  }
  if (method === 'GET' && /^\/posts\/\d+\/comments$/.test(path)) {
    return handleComments(store, idFrom(path, 2));
  }
  if (method === 'POST' && /^\/posts\/\d+\/comments$/.test(path)) {
    return handleAddComment(store, idFrom(path, 2), body || {}, req);
  }
  if (method === 'POST' && /^\/comments\/\d+\/moderate$/.test(path)) {
    return handleModerate(store, idFrom(path, 2), body || {}, req);
  }
  if (method === 'GET' && path.startsWith('/posts/by-slug/')) {
    return handleSlug(store, path.split('/')[3]);
  }
  if (method === 'GET' && path.startsWith('/posts/')) {
    return handleGet(store, idFrom(path, 2));
  }
  if (method === 'POST' && path === '/posts') {
    return handleCreate(store, body, req);
  }
  if (method === 'DELETE' && path.startsWith('/posts/')) {
    return handleDelete(store, idFrom(path, 2), req);
  }
  if (method === 'POST' && path === '/login') {
    return handleLogin(store, body);
  }
  if (method === 'POST' && path === '/logout') {
    return handleLogout(req);
  }
  if (method === 'GET' && path.startsWith('/admin/report')) {
    return handleReport(store, req, path.endsWith('.csv') ? 'csv' : 'json');
  }
  if (method === 'GET' && path === '/export') {
    return handleExport(store);
  }
  return notFound(); // fallthrough
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 4fb9ce9b | 2026-09-11 | Ada Reyes | refactor | single not-found path | 1 |
