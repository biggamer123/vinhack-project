# Feature: export

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | bug fix (3), refactor (2), feature (1) |
| Scope | export |
| Active | 2026-02-26 to 2026-08-31 |
| Commits | 6 |
| People | Sam Okafor (5), Ada Reyes (1) |
| Ask first | Sam Okafor <sam@example.com> |
| Files | 2 |
| Functions | 4 |
| Highest risk | legacyRow - 9 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-02-26 **feature**: legacy csv export - Sam Okafor
- 2026-04-25 **refactor**: widen legacy rows - Ada Reyes
- 2026-07-04 **bug fix**: row width overflow - Sam Okafor
- 2026-07-24 **refactor**: configurable export width - Sam Okafor
- 2026-08-10 **bug fix**: long titles broke csv rows - Sam Okafor
- 2026-08-31 **bug fix**: width for the new templates - Sam Okafor

## Where the code lives

### Files

- `src/legacy.js` - 5 commits
- `src/api.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `legacyRow` | src/legacy.js:16-20 | 9 (low) | 1 | 3 | no data |
| `handleExport` | src/api.js:60-64 | 8 (low) | 2 | 4 | no data |
| `legacyExport` | src/legacy.js:6-14 | 7 (low) | 1 | 3 | no data |
| `legacyFlags` | src/legacy.js:22-31 | 7 (low) | 1 | 0 | no data |

## How it fits together

### Calls inside the feature

- `legacyRow` -> `legacyFlags`
- `handleExport` -> `legacyExport`
- `legacyExport` -> `legacyRow`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `handleExport` is called by `route` (src/api.js:138-183), `dump` (src/app.js:37-39)

### What the feature depends on

- `trace` (src/telemetry.js:3-9) - used by `legacyRow`, `handleExport`, `legacyExport`
- `truncate` (src/format.js:5-10) - used by `legacyRow`
- `recordChange` (src/audit.js:3-7) - used by `handleExport`, `legacyExport`
- `ok` (src/api.js:125-127) - used by `handleExport`

## Risks to keep in mind

- `legacyExport` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `legacyFlags` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1

## Source

### legacyRow - src/legacy.js:16-20

```javascript
function legacyRow(post, options) {
  trace('legacy.row', {});
  const width = options && options.width ? options.width : 44;
  return [post.id, truncate(post.title, width), legacyFlags(post)].join(',');
}
```

### handleExport - src/api.js:60-64

```javascript
function handleExport(store) {
  trace('api.export', {});
  recordChange('web', 'export', 'all');
  return ok(legacyExport(store, { width: 60 }));
}
```

### legacyExport - src/legacy.js:6-14

```javascript
function legacyExport(store, options) {
  trace('legacy.export', {});
  recordChange('legacy', 'export', 'all');
  const rows = [];
  for (const post of store.posts.values()) {
    rows.push(legacyRow(post, options));
  }
  return rows.join('\n');
}
```

### legacyFlags - src/legacy.js:22-31

```javascript
function legacyFlags(post) {
  const flags = [];
  if (post.tags && post.tags.length) {
    flags.push('tagged');
  }
  if (post.body && post.body.length > 500) {
    flags.push('long');
  }
  return flags.join('|');
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 640e2c07 | 2026-08-31 | Sam Okafor | bug fix | width for the new templates | 1 |
| e6287c70 | 2026-08-10 | Sam Okafor | bug fix | long titles broke csv rows | 1 |
| 09564b28 | 2026-07-24 | Sam Okafor | refactor | configurable export width | 1 |
| ccca3b2c | 2026-07-04 | Sam Okafor | bug fix | row width overflow | 1 |
| 7d390dda | 2026-04-25 | Ada Reyes | refactor | widen legacy rows | 1 |
| 7d82d803 | 2026-02-26 | Sam Okafor | feature | legacy csv export | 1 |
