# Feature: api server entrypoint

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | feature (1) |
| Scope | none - grouped by commit title |
| Active | 2026-08-12 to 2026-08-12 |
| Commits | 1 |
| People | Ada Reyes (1) |
| Ask first | Ada Reyes <ada@example.com> |
| Files | 1 |
| Functions | 2 |
| Highest risk | readBody - 8 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-08-12 **feature**: api server entrypoint - Ada Reyes

## Where the code lives

### Files

- `src/server.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `readBody` | src/server.js:6-18 | 8 (low) | 1 | 0 | no data |
| `start` | src/server.js:20-31 | 8 (low) | 1 | 6 | no data |

## How it fits together

### Calls inside the feature

- `start` -> `readBody`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `start` is called by `paginateV1` (src/utils/deprecated.js:25-28)

### What the feature depends on

- `loadConfig` (src/config.js:23-33) - used by `start`
- `newServer` (src/api.js:185-190) - used by `start`
- `request` (web/src/api/client.ts:26-36) - used by `start`
- `route` (src/api.js:138-183) - used by `start`
- `trace` (src/telemetry.js:3-9) - used by `start`

## Risks to keep in mind

- `readBody` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `start` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1

## Source

### readBody - src/server.js:6-18

```javascript
function readBody(request) {
  return new Promise((resolve) => {
    let raw = '';
    request.on('data', (chunk) => (raw += chunk));
    request.on('end', () => {
      try {
        resolve(raw ? JSON.parse(raw) : null);
      } catch {
        resolve(null);
      }
    });
  });
}
```

### start - src/server.js:20-31

```javascript
function start() {
  const config = loadConfig();
  const app = newServer();
  const server = http.createServer(async (request, response) => {
    const body = await readBody(request);
    const result = app.route(app.store, request.method, request.url, body, request);
    response.writeHead(result.status, { 'content-type': 'application/json' });
    response.end(JSON.stringify(result.data));
  });
  server.listen(config.port, () => trace('server.listen', { port: config.port }));
  return server;
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 53ececc2 | 2026-08-12 | Ada Reyes | feature | api server entrypoint | 1 |
