# Feature: initial blog engine

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | feature (1) |
| Scope | none - grouped by commit title |
| Active | 2026-02-04 to 2026-02-04 |
| Commits | 1 |
| People | Ada Reyes (1) |
| Ask first | Ada Reyes <ada@example.com> |
| Files | 10 |
| Functions | 57 |
| Highest risk | ok - 31 (high) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-02-04 **feature**: initial blog engine - Ada Reyes

## Where the code lives

### Files

- `.gitignore` - 1 commit
- `package.json` - 1 commit
- `README.md` - 1 commit
- `src/api.js` - 1 commit
- `src/app.js` - 1 commit
- `src/format.js` - 1 commit
- `src/render.js` - 1 commit
- `src/store.js` - 1 commit
- `src/utils/deprecated.js` - 1 commit
- `src/validate.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `ok` | src/api.js:125-127 | 31 (high) | 13 | 0 | no data |
| `listPosts` | src/store.js:43-45 | 17 (medium) | 6 | 0 | no data |
| `renderCard` | src/render.js:4-10 | 16 (medium) | 5 | 3 | no data |
| `notFound` | src/api.js:129-132 | 15 (medium) | 5 | 1 | no data |
| `slugify` | src/format.js:1-3 | 13 (low) | 4 | 0 | no data |
| `truncate` | src/format.js:5-10 | 13 (low) | 4 | 0 | no data |
| `route` | src/api.js:138-183 | 12 (low) | 3 | 16 | no data |
| `newServer` | src/api.js:185-190 | 11 (low) | 3 | 3 | no data |
| `handle` | src/app.js:29-31 | 11 (low) | 3 | 1 | no data |
| `getPost` | src/store.js:34-36 | 11 (low) | 3 | 0 | no data |
| `handleSearch` | src/api.js:54-58 | 9 (low) | 2 | 5 | no data |
| `seed` | src/app.js:6-12 | 9 (low) | 2 | 2 | no data |
| `escapeHtml` | src/format.js:16-22 | 9 (low) | 2 | 0 | no data |
| `stripTags` | src/format.js:24-26 | 9 (low) | 2 | 1 | no data |
| `pluralize` | src/format.js:28-30 | 9 (low) | 2 | 0 | no data |
| `renderFeed` | src/render.js:31-34 | 9 (low) | 2 | 2 | no data |
| `renderPage` | src/render.js:36-41 | 9 (low) | 2 | 3 | no data |
| `savePost` | src/store.js:18-28 | 9 (low) | 2 | 5 | no data |
| `handleExport` | src/api.js:60-64 | 8 (low) | 2 | 4 | no data |
| `hasBody` | src/validate.js:7-9 | 8 (low) | 1 | 0 | no data |
| `handleGet` | src/api.js:14-22 | 7 (low) | 1 | 6 | no data |
| `handleList` | src/api.js:24-28 | 7 (low) | 1 | 5 | no data |
| `handleCreate` | src/api.js:30-39 | 7 (low) | 1 | 7 | no data |
| `handleDelete` | src/api.js:41-52 | 7 (low) | 1 | 7 | no data |
| `handleSlug` | src/api.js:66-71 | 7 (low) | 1 | 6 | no data |
| `handleComments` | src/api.js:73-76 | 7 (low) | 1 | 5 | no data |
| `handleAddComment` | src/api.js:78-85 | 7 (low) | 1 | 5 | no data |
| `handleModerate` | src/api.js:87-94 | 7 (low) | 1 | 6 | no data |
| `handleLogin` | src/api.js:96-105 | 7 (low) | 1 | 5 | no data |
| `handleLogout` | src/api.js:107-110 | 7 (low) | 1 | 3 | no data |
| `handleRss` | src/api.js:112-114 | 7 (low) | 1 | 1 | no data |
| `handleReport` | src/api.js:116-123 | 7 (low) | 1 | 5 | no data |
| `idFrom` | src/api.js:134-136 | 7 (low) | 1 | 0 | no data |
| `bootstrap` | src/app.js:14-18 | 7 (low) | 1 | 4 | no data |
| `titleCase` | src/format.js:12-14 | 7 (low) | 1 | 0 | no data |
| `renderHero` | src/render.js:12-17 | 7 (low) | 1 | 2 | no data |
| `renderList` | src/render.js:19-21 | 7 (low) | 1 | 1 | no data |
| `wrapFeed` | src/render.js:27-29 | 7 (low) | 1 | 0 | no data |
| `createStore` | src/store.js:6-8 | 7 (low) | 1 | 0 | no data |
| `indexPost` | src/store.js:10-16 | 7 (low) | 1 | 3 | no data |
| `nextId` | src/store.js:30-32 | 7 (low) | 1 | 0 | no data |
| `findBySlug` | src/store.js:38-41 | 7 (low) | 1 | 1 | no data |
| `deletePost` | src/store.js:47-56 | 7 (low) | 1 | 3 | no data |
| `hasTitle` | src/validate.js:3-5 | 7 (low) | 1 | 0 | no data |
| `isValidPost` | src/validate.js:11-16 | 7 (low) | 1 | 2 | no data |
| `sanitizePost` | src/validate.js:18-24 | 7 (low) | 1 | 1 | no data |
| `assertPost` | src/validate.js:26-31 | 7 (low) | 1 | 2 | no data |
| `preview` | src/app.js:20-22 | 5 (low) | 0 | 1 | no data |
| `constructor` | src/app.js:25-27 | 5 (low) | 0 | 2 | no data |
| `main` | src/app.js:42-46 | 5 (low) | 0 | 2 | no data |
| `find` | src/app.js:33-35 | -3 (unused) | 0 | 2 | no data |
| `dump` | src/app.js:37-39 | -3 (unused) | 0 | 1 | no data |
| `renderTagLine` | src/render.js:23-25 | -3 (unused) | 0 | 1 | no data |
| `oldSlugify` | src/utils/deprecated.js:9-11 | -3 (unused) | 0 | 0 | no data |
| `formatDateLegacy` | src/utils/deprecated.js:4-7 | -4 (unused) | 0 | 0 | no data |
| `paginateV1` | src/utils/deprecated.js:25-28 | -4 (unused) | 0 | 1 | no data |
| `retryWithBackoff` | src/utils/deprecated.js:13-23 | -11 (unused) | 0 | 0 | no data |

## How it fits together

### Calls inside the feature

- `renderCard` -> `slugify`
- `renderCard` -> `titleCase`
- `renderCard` -> `truncate`
- `route` -> `handleList`
- `route` -> `handleRss`
- `route` -> `handleSearch`
- `route` -> `handleComments`
- `route` -> `idFrom`
- `route` -> `handleAddComment`
- `route` -> `handleModerate`
- `route` -> `handleSlug`
- `route` -> `handleGet`
- `route` -> `handleCreate`
- `route` -> `handleDelete`
- `route` -> `handleLogin`
- `route` -> `handleLogout`
- `route` -> `handleReport`
- `route` -> `handleExport`
- `route` -> `notFound`
- `newServer` -> `createStore`
- `newServer` -> `route`
- `handle` -> `route`
- `handleSearch` -> `ok`
- `seed` -> `savePost`
- `stripTags` -> `escapeHtml`
- `renderFeed` -> `renderList`
- `renderFeed` -> `wrapFeed`
- `renderPage` -> `renderFeed`
- `renderPage` -> `renderHero`
- `savePost` -> `assertPost`
- `savePost` -> `nextId`
- `savePost` -> `indexPost`
- `handleExport` -> `ok`
- `handleGet` -> `getPost`
- `handleGet` -> `notFound`
- `handleGet` -> `ok`
- `handleGet` -> `renderCard`
- `handleList` -> `ok`
- `handleList` -> `renderFeed`
- `handleList` -> `listPosts`
- `handleCreate` -> `savePost`
- `handleCreate` -> `ok`
- `handleCreate` -> `renderCard`
- `handleDelete` -> `deletePost`
- `handleDelete` -> `notFound`
- `handleDelete` -> `ok`
- `handleSlug` -> `findBySlug`
- `handleSlug` -> `ok`
- `handleSlug` -> `renderCard`
- `handleSlug` -> `notFound`
- `handleComments` -> `ok`
- `handleAddComment` -> `ok`
- `handleModerate` -> `ok`
- `handleModerate` -> `notFound`
- `handleLogout` -> `ok`
- `handleReport` -> `ok`
- `bootstrap` -> `seed`
- `bootstrap` -> `newServer`
- `bootstrap` -> `renderPage`
- `renderHero` -> `renderCard`
- `renderHero` -> `escapeHtml`
- `renderList` -> `renderCard`
- `indexPost` -> `slugify`
- `findBySlug` -> `getPost`
- `deletePost` -> `getPost`
- `isValidPost` -> `hasTitle`
- `isValidPost` -> `hasBody`
- `sanitizePost` -> `stripTags`
- `assertPost` -> `isValidPost`
- `assertPost` -> `sanitizePost`
- `preview` -> `renderPage`
- `constructor` -> `seed`
- `constructor` -> `newServer`
- `main` -> `bootstrap`
- `main` -> `handle`
- `find` -> `handleSearch`
- `dump` -> `handleExport`
- `renderTagLine` -> `pluralize`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `ok` is called by `handleLogin` (web/src/components/Navbar.tsx:9-13)
- `listPosts` is called by `dailyReport` (src/admin/reports.js:14-23), `weeklyReportV1` (src/admin/reports.js:36-39), `buildRss` (src/feed/rss.js:25-30), `buildIndex` (src/search.js:9-20), `search` (src/search.js:40-43)
- `slugify` is called by `tokenize` (src/search.js:5-7), `normalizeTag` (src/tags.js:3-5)
- `truncate` is called by `addComment` (src/comments.js:18-37), `rssItem` (src/feed/rss.js:14-23), `legacyRow` (src/legacy.js:16-20)
- `route` is called by `start` (src/server.js:20-31)
- `newServer` is called by `start` (src/server.js:20-31)
- `handle` is called by `Run` (services/mailer/internal/queue/consumer.go:31-44), `drain` (services/mailer/internal/queue/consumer.go:46-59)
- `stripTags` is called by `addComment` (src/comments.js:18-37)
- `pluralize` is called by `formatDigestLine` (src/notifications.js:24-26)

### What the feature depends on

- `trace` (src/telemetry.js:3-9) - used by `notFound`, `handleSearch`, `seed`, `renderPage`, `savePost`, `handleExport`, `handleGet`, `handleList`, `handleCreate`, `handleDelete`, `handleSlug`, `handleComments`, `handleLogin`, `bootstrap`, `indexPost`, `deletePost`
- `createCommentStore` (src/comments.js:9-11) - used by `newServer`
- `query` (src/db/pool.js:9-17) - used by `handleSearch`, `find`
- `recordChange` (src/audit.js:3-7) - used by `handleSearch`, `savePost`, `handleExport`, `handleGet`, `handleList`, `handleCreate`, `handleDelete`, `handleSlug`, `handleLogin`, `indexPost`, `deletePost`
- `search` (src/search.js:40-43) - used by `handleSearch`
- `legacyExport` (src/legacy.js:6-14) - used by `handleExport`
- `requireRole` (src/auth/middleware.js:29-38) - used by `handleCreate`, `handleDelete`, `handleModerate`, `handleReport`
- `request` (web/src/api/client.ts:26-36) - used by `handleCreate`, `handleDelete`, `handleAddComment`, `handleModerate`, `handleReport`
- `threadComments` (src/comments.js:45-54) - used by `handleComments`
- `listComments` (src/comments.js:39-43) - used by `handleComments`
- `comments` (web/src/hooks/usePosts.ts:6) - used by `handleComments`, `handleAddComment`, `handleModerate`
- `requireUser` (src/auth/middleware.js:17-27) - used by `handleAddComment`
- `addComment` (src/comments.js:18-37) - used by `handleAddComment`
- `moderateComment` (src/comments.js:60-76) - used by `handleModerate`
- `verifyPassword` (src/auth/password.js:20-29) - used by `handleLogin`
- `createSession` (src/auth/session.js:16-24) - used by `handleLogin`
- `sessionCookie` (src/auth/session.js:48-51) - used by `handleLogin`
- `revokeSession` (src/auth/session.js:42-46) - used by `handleLogout`
- `tokenFromCookie` (src/auth/session.js:53-56) - used by `handleLogout`
- `buildRss` (src/feed/rss.js:25-30) - used by `handleRss`
- `dailyReport` (src/admin/reports.js:14-23) - used by `handleReport`
- `exportCsv` (src/admin/reports.js:30-33) - used by `handleReport`
- `start` (src/server.js:20-31) - used by `paginateV1`

## Risks to keep in mind

- `ok` - HIGH (31): 13 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `listPosts` - MEDIUM (17): 6 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `renderCard` - MEDIUM (16): 5 callers, coverage no data, 1 change in 90 days, bus factor 1
- `notFound` - MEDIUM (15): 5 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `slugify` - LOW (13): 4 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `truncate` - LOW (13): 4 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `route` - LOW (12): 3 callers, coverage no data, 1 change in 90 days, bus factor 1
- `newServer` - LOW (11): 3 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `handle` - LOW (11): 3 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `getPost` - LOW (11): 3 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `handleSearch` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `seed` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `escapeHtml` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `stripTags` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `pluralize` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `renderFeed` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `renderPage` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `savePost` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `hasBody` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `handleGet` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleList` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleCreate` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleDelete` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleSlug` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleComments` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleAddComment` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleModerate` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleLogin` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleLogout` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleRss` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `handleReport` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `idFrom` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `bootstrap` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `titleCase` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `renderHero` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `renderList` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `wrapFeed` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `createStore` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `indexPost` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `nextId` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `findBySlug` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `deletePost` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `hasTitle` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `isValidPost` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `sanitizePost` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `assertPost` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `preview` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `constructor` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `main` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `find` - UNUSED (-3): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `dump` - UNUSED (-3): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `renderTagLine` - UNUSED (-3): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `oldSlugify` - UNUSED (-3): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `formatDateLegacy` - UNUSED (-4): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `paginateV1` - UNUSED (-4): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `retryWithBackoff` - UNUSED (-11): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1

## Source

### ok - src/api.js:125-127

```javascript
function ok(data) {
  return { status: 200, data };
}
```

### listPosts - src/store.js:43-45

```javascript
function listPosts(store) {
  return [...store.posts.values()];
}
```

### renderCard - src/render.js:4-10

```javascript
function renderCard(post) {
  return {
    id: slugify(post.title),
    heading: titleCase(post.title),
    blurb: truncate(post.body, 140),
  };
}
```

### notFound - src/api.js:129-132

```javascript
function notFound() {
  trace('api.404', {});
  return { status: 404, data: null };
}
```

### slugify - src/format.js:1-3

```javascript
function slugify(text) {
  return text.toLowerCase().trim().replace(/[^\w\s-]/g, '').replace(/\s+/g, '-');
}
```

### truncate - src/format.js:5-10

```javascript
function truncate(text, max) {
  if (text.length <= max) {
    return text;
  }
  return text.slice(0, max).trimEnd() + '...';
}
```

### route - src/api.js:138-183

```javascript
function route(store, method, path, body, request) {
  const req = request || { headers: {} };
  if (method === 'GET' && path === '/posts') {
    return handleList(store);
  }
  if (method === 'GET' && path === '/feed.xml') {
    return handleRss(store);
  }
  if (method === 'GET' && path.startsWith('/search?q=')) {
    return handleSearch(store, decodeURIComponent(path.slice(10)));
  }
  if (method === 'GET' && /^\/posts\/\d+\/comments$/.test(path)) {
    return handleComments(store, idFrom(path, 2));
  }
  if (method === 'POST' && /^\/posts\/\d+\/comments$/.test(path)) {
    return handleAddComment(store, idFrom(path, 2), body || {}, req);
  }
  if (method === 'POST' && /^\/comments\/\d+\/moderate$/.test(path)) {
    return handleModerate(store, idFrom(path, 2), body || {}, req);
  }
  if (method === 'GET' && path.startsWith('/posts/by-slug/')) {
    return handleSlug(store, path.split('/')[3]);
  }
  if (method === 'GET' && path.startsWith('/posts/')) {
    return handleGet(store, idFrom(path, 2));
  }
  if (method === 'POST' && path === '/posts') {
    return handleCreate(store, body, req);
  }
  if (method === 'DELETE' && path.startsWith('/posts/')) {
    return handleDelete(store, idFrom(path, 2), req);
  }
  if (method === 'POST' && path === '/login') {
    return handleLogin(store, body);
  }
  if (method === 'POST' && path === '/logout') {
    return handleLogout(req);
  }
  if (method === 'GET' && path.startsWith('/admin/report')) {
    return handleReport(store, req, path.endsWith('.csv') ? 'csv' : 'json');
  }
  if (method === 'GET' && path === '/export') {
    return handleExport(store);
  }
  return notFound(); // fallthrough
}
```

### newServer - src/api.js:185-190

```javascript
function newServer() {
  const store = createStore();
  store.comments = createCommentStore();
  store.users = new Map();
  return { store, route };
}
```

### handle - src/app.js:29-31

```javascript
  handle(method, path, body) {
    return route(this.server.store, method, path, body);
  }
```

### getPost - src/store.js:34-36

```javascript
function getPost(store, id) {
  return store.posts.get(id) || null;
}
```

### handleSearch - src/api.js:54-58

```javascript
function handleSearch(store, query) {
  trace('api.search', { query });
  recordChange('web', 'search', query);
  return ok(search(store, query));
}
```

### seed - src/app.js:6-12

```javascript
function seed(server, posts) {
  trace('app.seed', {});
  for (const post of posts) {
    savePost(server.store, post);
  }
  return server;
}
```

### escapeHtml - src/format.js:16-22

```javascript
function escapeHtml(text) {
  return String(text)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}
```

### stripTags - src/format.js:24-26

```javascript
function stripTags(html) {
  return escapeHtml(html).replace(/<[^>]*>/g, '');
}
```

### pluralize - src/format.js:28-30

```javascript
function pluralize(word, count) {
  return count === 1 ? word : word + 's'; // naive
}
```

### renderFeed - src/render.js:31-34

```javascript
function renderFeed(posts) {
  const cards = renderList(posts);
  return wrapFeed(cards);
}
```

### renderPage - src/render.js:36-41

```javascript
function renderPage(posts) {
  trace('render.page', {});
  const feed = renderFeed(posts);
  const hero = renderHero(posts[0]);
  return { hero, feed };
}
```

### savePost - src/store.js:18-28

```javascript
function savePost(store, raw) {
  trace('store.save', {});
  recordChange('store', 'save', 'post');
  const post = assertPost(raw);
  post.id = nextId(store);
  post.createdAt = Date.now();
  store.posts.set(post.id, post);
  indexPost(store, post);
  // TODO: reindex on update too
  return post;
}
```

### handleExport - src/api.js:60-64

```javascript
function handleExport(store) {
  trace('api.export', {});
  recordChange('web', 'export', 'all');
  return ok(legacyExport(store, { width: 60 }));
}
```

### hasBody - src/validate.js:7-9

```javascript
function hasBody(post) {
  return typeof post.body === 'string' && post.body.length > 10;
}
```

### handleGet - src/api.js:14-22

```javascript
function handleGet(store, id) {
  trace('api.get', { id });
  recordChange('web', 'read', id);
  const post = getPost(store, id);
  if (!post) {
    return notFound();
  }
  return ok(renderCard(post));
}
```

### handleList - src/api.js:24-28

```javascript
function handleList(store) {
  trace('api.list', {});
  recordChange('web', 'list', 'posts');
  return ok(renderFeed(listPosts(store)));
}
```

### handleCreate - src/api.js:30-39

```javascript
function handleCreate(store, body, request) {
  trace('api.create', { body });
  const denied = requireRole(request, 'author');
  if (denied) {
    return denied;
  }
  recordChange(request.session.userId, 'create', 'post');
  const post = savePost(store, body);
  return ok(renderCard(post));
}
```

### handleDelete - src/api.js:41-52

```javascript
function handleDelete(store, id, request) {
  trace('api.delete', { id });
  const denied = requireRole(request, 'editor');
  if (denied) {
    return denied;
  }
  recordChange(request.session.userId, 'delete', id);
  if (!deletePost(store, id)) {
    return notFound();
  }
  return ok({ deleted: id });
}
```

### handleSlug - src/api.js:66-71

```javascript
function handleSlug(store, slug) {
  trace('api.slug', { slug });
  recordChange('web', 'read', slug);
  const post = findBySlug(store, slug);
  return post ? ok(renderCard(post)) : notFound();
}
```

### handleComments - src/api.js:73-76

```javascript
function handleComments(store, postId) {
  trace('api.comments', { postId });
  return ok(threadComments(listComments(store.comments, postId)));
}
```

### handleAddComment - src/api.js:78-85

```javascript
function handleAddComment(store, postId, body, request) {
  const denied = requireUser(request);
  if (denied) {
    return denied;
  }
  const author = { id: request.session.userId, name: body.name || 'reader' };
  return ok(addComment(store.comments, postId, author, body.text, body.parentId));
}
```

### handleModerate - src/api.js:87-94

```javascript
function handleModerate(store, commentId, body, request) {
  const denied = requireRole(request, 'editor');
  if (denied) {
    return denied;
  }
  const comment = moderateComment(store.comments, commentId, body.decision, { id: request.session.userId });
  return comment ? ok(comment) : notFound();
}
```

### handleLogin - src/api.js:96-105

```javascript
function handleLogin(store, body) {
  trace('api.login', { email: body && body.email });
  const user = store.users.get(body && body.email);
  if (!user || !verifyPassword(body.password, user.passwordHash)) {
    recordChange('anon', 'login-failed', body && body.email);
    return { status: 401, data: { error: 'invalid credentials' } };
  }
  const session = createSession(user);
  return { status: 200, data: { ok: true }, headers: { 'set-cookie': sessionCookie(session) } };
}
```

### handleLogout - src/api.js:107-110

```javascript
function handleLogout(request) {
  revokeSession(tokenFromCookie(request.headers && request.headers.cookie));
  return ok({ loggedOut: true });
}
```

### handleRss - src/api.js:112-114

```javascript
function handleRss(store) {
  return { status: 200, data: buildRss(store, 'https://inkwell.example') };
}
```

### handleReport - src/api.js:116-123

```javascript
function handleReport(store, request, format) {
  const denied = requireRole(request, 'admin');
  if (denied) {
    return denied;
  }
  const report = dailyReport(store);
  return format === 'csv' ? ok(exportCsv(report.authors.map(([id, posts]) => ({ id, posts })), ['id', 'posts'])) : ok(report);
}
```

### idFrom - src/api.js:134-136

```javascript
function idFrom(path, index) {
  return Number(path.split('/')[index]);
}
```

### bootstrap - src/app.js:14-18

```javascript
function bootstrap(posts) {
  trace('app.boot', {});
  const server = seed(newServer(), posts);
  return renderPage(posts);
}
```

### titleCase - src/format.js:12-14

```javascript
const titleCase = (text) => {
  return text.replace(/\b\w/g, (c) => c.toUpperCase());
};
```

### renderHero - src/render.js:12-17

```javascript
function renderHero(post) {
  const card = renderCard(post);
  card.hero = true;
  card.banner = escapeHtml(post.title);
  return card;
}
```

### renderList - src/render.js:19-21

```javascript
function renderList(posts) {
  return posts.map((post) => renderCard(post));
}
```

### wrapFeed - src/render.js:27-29

```javascript
function wrapFeed(cards) {
  return { count: cards.length, cards };
}
```

### createStore - src/store.js:6-8

```javascript
function createStore() {
  return { posts: new Map(), index: new Map() };
}
```

### indexPost - src/store.js:10-16

```javascript
function indexPost(store, post) {
  trace('store.index', {});
  recordChange('store', 'index', 'post');
  const key = slugify(post.title);
  store.index.set(key, post.id);
  return key;
}
```

### nextId - src/store.js:30-32

```javascript
function nextId(store) {
  return store.posts.size + 1;
}
```

### findBySlug - src/store.js:38-41

```javascript
function findBySlug(store, slug) {
  const id = store.index.get(slug);
  return getPost(store, id);
}
```

### deletePost - src/store.js:47-56

```javascript
function deletePost(store, id) {
  trace('store.delete', { id });
  recordChange('store', 'delete', id);
  const post = getPost(store, id);
  if (!post) {
    return false;
  }
  store.posts.delete(id);
  return true;
}
```

### hasTitle - src/validate.js:3-5

```javascript
function hasTitle(post) {
  return typeof post.title === 'string' && post.title.length > 0;
}
```

### isValidPost - src/validate.js:11-16

```javascript
function isValidPost(post) {
  if (!post) {
    return false;
  }
  return hasTitle(post) && hasBody(post);
}
```

### sanitizePost - src/validate.js:18-24

```javascript
function sanitizePost(post) {
  return {
    title: stripTags(post.title),
    body: stripTags(post.body),
    tags: post.tags || [],
  };
}
```

### assertPost - src/validate.js:26-31

```javascript
function assertPost(post) {
  if (!isValidPost(post)) {
    throw new Error('invalid post');
  }
  return sanitizePost(post);
}
```

### preview - src/app.js:20-22

```javascript
const preview = function (post) {
  return renderPage([post]);
};
```

### constructor - src/app.js:25-27

```javascript
  constructor(posts) {
    this.server = seed(newServer(), posts);
  }
```

### main - src/app.js:42-46

```javascript
module.exports.main = (posts) => {
  const app = new Application(posts);
  bootstrap(posts);
  return app.handle('GET', '/posts');
};
```

### find - src/app.js:33-35

```javascript
  find(query) {
    return handleSearch(this.server.store, query);
  }
```

### dump - src/app.js:37-39

```javascript
  dump() {
    return handleExport(this.server.store);
  }
```

### renderTagLine - src/render.js:23-25

```javascript
function renderTagLine(tags) {
  return tags.length + ' ' + pluralize('tag', tags.length);
}
```

### oldSlugify - src/utils/deprecated.js:9-11

```javascript
function oldSlugify(text) {
  return String(text).toLowerCase().split(' ').join('_');
}
```

### formatDateLegacy - src/utils/deprecated.js:4-7

```javascript
function formatDateLegacy(timestamp) {
  const d = new Date(timestamp);
  return `${d.getMonth() + 1}/${d.getDate()}/${d.getFullYear()}`;
}
```

### paginateV1 - src/utils/deprecated.js:25-28

```javascript
function paginateV1(items, page, size) {
  const start = (page - 1) * size;
  return { items: items.slice(start, start + size), page, pages: Math.ceil(items.length / size) };
}
```

### retryWithBackoff - src/utils/deprecated.js:13-23

```javascript
function retryWithBackoff(fn, attempts) {
  let lastError;
  for (let i = 0; i < (attempts || 3); i++) {
    try {
      return fn();
    } catch (err) {
      lastError = err;
    }
  }
  throw lastError;
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| c3cd66ba | 2026-02-04 | Ada Reyes | feature | initial blog engine | 10 |
