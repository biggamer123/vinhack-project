# Feature: rate-limit

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | performance (1), hotfix (1), feature (1) |
| Scope | rate-limit |
| Active | 2026-08-06 to 2026-08-26 |
| Commits | 3 |
| People | Mina Kowalski (2), Sam Okafor (1) |
| Ask first | Mina Kowalski <mina@example.com> |
| Files | 1 |
| Functions | 3 |
| Highest risk | bucketFor - 8 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-08-06 **feature**: token bucket per client - Mina Kowalski
- 2026-08-14 **hotfix**: capacity too low for feed readers - Mina Kowalski
- 2026-08-26 **performance**: lower capacity, faster refill - Sam Okafor

## Where the code lives

### Files

- `src/rateLimit.js` - 3 commits

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `bucketFor` | src/rateLimit.js:6-13 | 8 (low) | 1 | 0 | no data |
| `refill` | src/rateLimit.js:15-20 | 8 (low) | 1 | 0 | no data |
| `allowRequest` | src/rateLimit.js:22-29 | 8 (low) | 1 | 2 | no data |

## How it fits together

### Calls inside the feature

- `allowRequest` -> `refill`
- `allowRequest` -> `bucketFor`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `allowRequest` is called by `requireUser` (src/auth/middleware.js:17-27)

### What the feature depends on

- Nothing outside the feature.

## Risks to keep in mind

- `bucketFor` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `refill` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1
- `allowRequest` - LOW (8): 1 caller, coverage no data, 1 change in 90 days, bus factor 1

## Source

### bucketFor - src/rateLimit.js:6-13

```javascript
function bucketFor(client) {
  let bucket = buckets.get(client);
  if (!bucket) {
    bucket = { tokens: CAPACITY, updatedAt: Date.now() };
    buckets.set(client, bucket);
  }
  return bucket;
}
```

### refill - src/rateLimit.js:15-20

```javascript
function refill(bucket, now) {
  const elapsed = (now - bucket.updatedAt) / 1000;
  bucket.tokens = Math.min(CAPACITY, bucket.tokens + elapsed * REFILL_PER_SECOND);
  bucket.updatedAt = now;
  return bucket;
}
```

### allowRequest - src/rateLimit.js:22-29

```javascript
function allowRequest(client) {
  const bucket = refill(bucketFor(client), Date.now());
  if (bucket.tokens < 1) {
    return false;
  }
  bucket.tokens -= 1;
  return true;
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 52cdc6f3 | 2026-08-26 | Sam Okafor | performance | lower capacity, faster refill | 1 |
| 0f98238a | 2026-08-14 | Mina Kowalski | hotfix | capacity too low for feed readers | 1 |
| 2098c993 | 2026-08-06 | Mina Kowalski | feature | token bucket per client | 1 |
