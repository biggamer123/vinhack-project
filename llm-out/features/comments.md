# Feature: comments

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | refactor (1), bug fix (1), feature (3), test (1) |
| Scope | comments |
| Active | 2026-06-18 to 2026-09-09 |
| Commits | 6 |
| People | Jonah Lee (3), Leo Park (3) |
| Ask first | Jonah Lee <jonah@example.com> |
| Files | 6 |
| Functions | 14 |
| Highest risk | addComment - 9 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-06-18 **feature**: threaded comments api - Leo Park
- 2026-06-20 **feature**: spam filter - Jonah Lee
- 2026-06-22 **test**: threading, spam and cache - Leo Park
- 2026-06-23 **feature**: notify followers - Leo Park
- 2026-08-29 **bug fix**: stricter link spam threshold - Jonah Lee
- 2026-09-09 **refactor**: extend blocked word list - Jonah Lee

## Where the code lives

### Files

- `src/comments.js` - 4 commits
- `src/notifications.js` - 2 commits
- `db/migrations/002_comments.sql` - 1 commit
- `src/models/notification.model.js` - 1 commit
- `test/cache.test.js` - 1 commit
- `test/comments.test.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `addComment` | src/comments.js:18-37 | 9 (low) | 2 | 8 | no data |
| `queueEmail` | src/notifications.js:11-16 | 9 (low) | 2 | 1 | no data |
| `createCommentStore` | src/comments.js:9-11 | 7 (low) | 1 | 0 | no data |
| `listComments` | src/comments.js:39-43 | 7 (low) | 1 | 2 | no data |
| `threadComments` | src/comments.js:45-54 | 7 (low) | 1 | 1 | no data |
| `attach` | src/comments.js:51-52 | 7 (low) | 1 | 0 | no data |
| `moderateComment` | src/comments.js:60-76 | 7 (low) | 1 | 4 | no data |
| `notifyFollowers` | src/notifications.js:18-22 | 7 (low) | 1 | 2 | no data |
| `formatDigestLine` | src/notifications.js:24-26 | 7 (low) | 1 | 2 | no data |
| `isSpam` | src/comments.js:13-16 | 6 (low) | 1 | 0 | no data |
| `countReplies` | src/comments.js:56-58 | 5 (low) | 0 | 0 | no data |
| `follow` | src/notifications.js:7-9 | -3 (unused) | 0 | 0 | no data |
| `drainOutbox` | src/notifications.js:37-39 | -3 (unused) | 0 | 0 | no data |
| `buildDigest` | src/notifications.js:28-35 | -8 (unused) | 0 | 3 | no data |

## How it fits together

### Calls inside the feature

- `addComment` -> `isSpam`
- `addComment` -> `notifyFollowers`
- `threadComments` -> `attach`
- `notifyFollowers` -> `queueEmail`
- `buildDigest` -> `formatDigestLine`
- `buildDigest` -> `queueEmail`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `addComment` is called by `handleAddComment` (src/api.js:78-85), `mutationFn` (web/src/hooks/usePosts.ts:37)
- `createCommentStore` is called by `newServer` (src/api.js:185-190)
- `listComments` is called by `handleComments` (src/api.js:73-76)
- `threadComments` is called by `handleComments` (src/api.js:73-76)
- `moderateComment` is called by `handleModerate` (src/api.js:87-94)

### What the feature depends on

- `trace` (src/telemetry.js:3-9) - used by `addComment`, `queueEmail`, `moderateComment`, `notifyFollowers`, `buildDigest`
- `recordChange` (src/audit.js:3-7) - used by `addComment`, `moderateComment`
- `truncate` (src/format.js:5-10) - used by `addComment`
- `stripTags` (src/format.js:24-26) - used by `addComment`
- `invalidatePrefix` (src/cache.js:29-38) - used by `addComment`, `moderateComment`
- `cacheKey` (src/cache.js:4-6) - used by `addComment`, `listComments`, `moderateComment`
- `withCache` (src/cache.js:40-46) - used by `listComments`
- `pluralize` (src/format.js:28-30) - used by `formatDigestLine`
- `comments` (web/src/hooks/usePosts.ts:6) - used by `formatDigestLine`

## Risks to keep in mind

- `addComment` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `queueEmail` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `createCommentStore` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `listComments` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `threadComments` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `attach` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `moderateComment` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `notifyFollowers` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `formatDigestLine` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `countReplies` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `follow` - UNUSED (-3): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `drainOutbox` - UNUSED (-3): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `buildDigest` - UNUSED (-8): 0 callers, coverage no data, 1 change in 90 days, bus factor 1

## Source

### addComment - src/comments.js:18-37

```javascript
function addComment(store, postId, author, body, parentId) {
  trace('comments.add', { postId });
  recordChange(author.id, 'comment', postId);
  const clean = truncate(stripTags(body), 2000);
  const comment = {
    id: store.nextId++,
    postId,
    parentId: parentId || null,
    authorId: author.id,
    body: clean,
    status: isSpam(clean) ? 'held' : 'visible',
    createdAt: Date.now(),
  };
  store.comments.set(comment.id, comment);
  invalidatePrefix(cacheKey('comments', postId));
  if (comment.status === 'visible') {
    notifyFollowers(postId, `${author.name} commented`);
  }
  return comment;
}
```

### queueEmail - src/notifications.js:11-16

```javascript
function queueEmail(to, subject, body) {
  trace('mail.queue', { to });
  const job = { to, subject, body, queuedAt: Date.now(), attempts: 0 };
  outbox.push(job);
  return job;
}
```

### createCommentStore - src/comments.js:9-11

```javascript
function createCommentStore() {
  return { comments: new Map(), nextId: 1 };
}
```

### listComments - src/comments.js:39-43

```javascript
function listComments(store, postId) {
  return withCache(cacheKey('comments', postId, 'list'), 30000, () =>
    [...store.comments.values()].filter((c) => c.postId === postId && c.status === 'visible'),
  );
}
```

### threadComments - src/comments.js:45-54

```javascript
function threadComments(comments) {
  const byParent = new Map();
  for (const comment of comments) {
    const key = comment.parentId || 0;
    byParent.set(key, [...(byParent.get(key) || []), comment]);
  }
  const attach = (parentId) =>
    (byParent.get(parentId) || []).map((c) => ({ ...c, replies: attach(c.id) }));
  return attach(0);
}
```

### attach - src/comments.js:51-52

```javascript
  const attach = (parentId) =>
    (byParent.get(parentId) || []).map((c) => ({ ...c, replies: attach(c.id) }));
```

### moderateComment - src/comments.js:60-76

```javascript
function moderateComment(store, id, decision, moderator) {
  trace('comments.moderate', { id, decision });
  recordChange(moderator.id, `moderate:${decision}`, id);
  const comment = store.comments.get(id);
  if (!comment) {
    return null;
  }
  if (decision === 'approve') {
    comment.status = 'visible';
  } else if (decision === 'reject') {
    comment.status = 'removed';
  } else {
    throw new Error(`unknown decision ${decision}`);
  }
  invalidatePrefix(cacheKey('comments', comment.postId));
  return comment;
}
```

### notifyFollowers - src/notifications.js:18-22

```javascript
function notifyFollowers(postId, message) {
  trace('notify.followers', { postId });
  const emails = followers.get(postId) || [];
  return emails.map((email) => queueEmail(email, 'New activity on a post you follow', message));
}
```

### formatDigestLine - src/notifications.js:24-26

```javascript
function formatDigestLine(item) {
  return `- ${item.title} (${item.comments} ${pluralize('comment', item.comments)})`;
}
```

### isSpam - src/comments.js:13-16

```javascript
function isSpam(body) {
  const lower = body.toLowerCase();
  return BLOCKED_WORDS.some((word) => lower.includes(word)) || (lower.match(/https?:\/\//g) || []).length > 2;
}
```

### countReplies - src/comments.js:56-58

```javascript
function countReplies(thread) {
  return thread.reduce((sum, c) => sum + 1 + countReplies(c.replies || []), 0);
}
```

### follow - src/notifications.js:7-9

```javascript
function follow(postId, userEmail) {
  followers.set(postId, [...new Set([...(followers.get(postId) || []), userEmail])]);
}
```

### drainOutbox - src/notifications.js:37-39

```javascript
function drainOutbox(limit) {
  return outbox.splice(0, limit || outbox.length);
}
```

### buildDigest - src/notifications.js:28-35

```javascript
function buildDigest(user, items) {
  trace('notify.digest', { user: user.id });
  if (!items.length) {
    return null;
  }
  const lines = items.map(formatDigestLine);
  return queueEmail(user.email, `Your weekly Inkwell digest`, lines.join('\n'));
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 0eefc11b | 2026-09-09 | Jonah Lee | refactor | extend blocked word list | 1 |
| f1af2020 | 2026-08-29 | Jonah Lee | bug fix | stricter link spam threshold | 1 |
| 32e06891 | 2026-06-23 | Leo Park | feature | notify followers | 2 |
| 6fa3ab1c | 2026-06-22 | Leo Park | test | threading, spam and cache | 2 |
| 6156d803 | 2026-06-20 | Jonah Lee | feature | spam filter | 1 |
| bb45b7ed | 2026-06-18 | Leo Park | feature | threaded comments api | 3 |
