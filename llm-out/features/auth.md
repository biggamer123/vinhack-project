# Feature: auth

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | bug fix (1), feature (2), security (4), test (1) |
| Scope | auth |
| Active | 2026-03-26 to 2026-09-07 |
| Commits | 8 |
| People | Priya Nair (8) |
| Ask first | Priya Nair <priya@example.com> |
| Files | 5 |
| Functions | 23 |
| Highest risk | requireRole - 13 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-03-26 **security**: pbkdf2 password hashing - Priya Nair
- 2026-03-31 **feature**: sessions and role middleware - Priya Nair
- 2026-04-05 **test**: password hashing - Priya Nair
- 2026-07-08 **security**: httponly session cookie - Priya Nair
- 2026-07-12 **security**: secure and samesite cookie flags - Priya Nair
- 2026-07-27 **security**: raise pbkdf2 iterations - Priya Nair
- 2026-08-08 **feature**: editor role between author and admin - Priya Nair - BREAKING CHANGE
- 2026-09-07 **bug fix**: session ttl back to three days - Priya Nair

## Where the code lives

### Files

- `src/auth/session.js` - 4 commits
- `src/auth/middleware.js` - 2 commits
- `src/auth/password.js` - 2 commits
- `src/cache.js` - 1 commit
- `test/auth/password.test.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `requireRole` | src/auth/middleware.js:29-38 | 13 (low) | 4 | 3 | no data |
| `sessionKey` | src/auth/session.js:12-14 | 11 (low) | 3 | 0 | no data |
| `cacheKey` | src/cache.js:4-6 | 11 (low) | 3 | 0 | no data |
| `sessionCookie` | src/auth/session.js:48-51 | 9 (low) | 1 | 0 | no data |
| `forbidden` | src/auth/middleware.js:7-10 | 9 (low) | 2 | 1 | no data |
| `requireUser` | src/auth/middleware.js:17-27 | 9 (low) | 2 | 4 | no data |
| `tokenFromCookie` | src/auth/session.js:53-56 | 9 (low) | 2 | 0 | no data |
| `cacheGet` | src/cache.js:8-18 | 9 (low) | 2 | 0 | no data |
| `cacheSet` | src/cache.js:20-23 | 9 (low) | 2 | 0 | no data |
| `cacheDelete` | src/cache.js:25-27 | 9 (low) | 2 | 0 | no data |
| `invalidatePrefix` | src/cache.js:29-38 | 9 (low) | 2 | 0 | no data |
| `parseHash` | src/auth/password.js:12-18 | 9 (low) | 2 | 0 | no data |
| `withCache` | src/cache.js:40-46 | 8 (low) | 2 | 2 | no data |
| `currentUser` | src/auth/middleware.js:12-15 | 7 (low) | 1 | 2 | no data |
| `newToken` | src/auth/session.js:8-10 | 7 (low) | 1 | 0 | no data |
| `createSession` | src/auth/session.js:16-24 | 7 (low) | 1 | 5 | no data |
| `isExpired` | src/auth/session.js:26-28 | 7 (low) | 1 | 0 | no data |
| `readSession` | src/auth/session.js:30-40 | 7 (low) | 1 | 4 | no data |
| `revokeSession` | src/auth/session.js:42-46 | 7 (low) | 1 | 4 | no data |
| `verifyPassword` | src/auth/password.js:20-29 | 7 (low) | 1 | 1 | no data |
| `hashPassword` | src/auth/password.js:6-10 | 5 (low) | 0 | 0 | no data |
| `needsRehash` | src/auth/password.js:31-34 | 5 (low) | 0 | 1 | no data |
| `isStrongPassword` | src/auth/password.js:36-38 | 5 (low) | 0 | 0 | no data |

## How it fits together

### Calls inside the feature

- `requireRole` -> `requireUser`
- `requireRole` -> `forbidden`
- `requireUser` -> `currentUser`
- `requireUser` -> `forbidden`
- `withCache` -> `cacheGet`
- `withCache` -> `cacheSet`
- `currentUser` -> `tokenFromCookie`
- `currentUser` -> `readSession`
- `createSession` -> `newToken`
- `createSession` -> `cacheSet`
- `createSession` -> `sessionKey`
- `readSession` -> `cacheGet`
- `readSession` -> `sessionKey`
- `readSession` -> `isExpired`
- `readSession` -> `cacheDelete`
- `revokeSession` -> `cacheDelete`
- `revokeSession` -> `sessionKey`
- `verifyPassword` -> `parseHash`
- `needsRehash` -> `parseHash`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `requireRole` is called by `handleCreate` (src/api.js:30-39), `handleDelete` (src/api.js:41-52), `handleModerate` (src/api.js:87-94), `handleReport` (src/api.js:116-123)
- `cacheKey` is called by `addComment` (src/comments.js:18-37), `listComments` (src/comments.js:39-43), `moderateComment` (src/comments.js:60-76)
- `sessionCookie` is called by `handleLogin` (src/api.js:96-105)
- `requireUser` is called by `handleAddComment` (src/api.js:78-85)
- `tokenFromCookie` is called by `handleLogout` (src/api.js:107-110)
- `invalidatePrefix` is called by `addComment` (src/comments.js:18-37), `moderateComment` (src/comments.js:60-76)
- `withCache` is called by `listComments` (src/comments.js:39-43), `buildRss` (src/feed/rss.js:25-30)
- `createSession` is called by `handleLogin` (src/api.js:96-105)
- `revokeSession` is called by `handleLogout` (src/api.js:107-110)
- `verifyPassword` is called by `handleLogin` (src/api.js:96-105)

### What the feature depends on

- `request` (web/src/api/client.ts:26-36) - used by `requireRole`, `requireUser`
- `trace` (src/telemetry.js:3-9) - used by `forbidden`, `createSession`, `revokeSession`
- `allowRequest` (src/rateLimit.js:22-29) - used by `requireUser`
- `recordChange` (src/audit.js:3-7) - used by `createSession`, `revokeSession`

## Risks to keep in mind

- `requireRole` - LOW (13): 4 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `sessionKey` - LOW (11): 3 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `cacheKey` - LOW (11): 3 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `sessionCookie` - LOW (9): 1 caller, coverage no data, 2 changes in 90 days, bus factor 1
- `forbidden` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `requireUser` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `tokenFromCookie` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `cacheGet` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `cacheSet` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `cacheDelete` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `invalidatePrefix` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `parseHash` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `currentUser` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `newToken` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `createSession` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `isExpired` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `readSession` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `revokeSession` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `verifyPassword` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `hashPassword` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `needsRehash` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `isStrongPassword` - LOW (5): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1

## Source

### requireRole - src/auth/middleware.js:29-38

```javascript
function requireRole(request, role) {
  const denied = requireUser(request);
  if (denied) {
    return denied;
  }
  if ((ROLE_RANK[request.session.role] || 0) < ROLE_RANK[role]) {
    return forbidden(`needs ${role}`);
  }
  return null;
}
```

### sessionKey - src/auth/session.js:12-14

```javascript
function sessionKey(token) {
  return `session:${token}`;
}
```

### cacheKey - src/cache.js:4-6

```javascript
function cacheKey(...parts) {
  return parts.map((p) => String(p).replace(/:/g, '_')).join(':');
}
```

### sessionCookie - src/auth/session.js:48-51

```javascript
function sessionCookie(session) {
  const maxAge = Math.max(0, Math.floor((session.expiresAt - Date.now()) / 1000));
  return `sid=${session.token}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=${maxAge}`;
}
```

### forbidden - src/auth/middleware.js:7-10

```javascript
function forbidden(reason) {
  trace('auth.forbidden', { reason });
  return { status: 403, data: { error: reason } };
}
```

### requireUser - src/auth/middleware.js:17-27

```javascript
function requireUser(request) {
  if (!allowRequest(request.ip || 'anon')) {
    return { status: 429, data: { error: 'slow down' } };
  }
  const session = currentUser(request);
  if (!session) {
    return forbidden('login required');
  }
  request.session = session;
  return null;
}
```

### tokenFromCookie - src/auth/session.js:53-56

```javascript
function tokenFromCookie(header) {
  const match = /(?:^|;\s*)sid=([^;]+)/.exec(header || '');
  return match ? match[1] : null;
}
```

### cacheGet - src/cache.js:8-18

```javascript
function cacheGet(key) {
  const entry = entries.get(key);
  if (!entry) {
    return null;
  }
  if (entry.expiresAt && entry.expiresAt < Date.now()) {
    entries.delete(key);
    return null;
  }
  return entry.value;
}
```

### cacheSet - src/cache.js:20-23

```javascript
function cacheSet(key, value, ttlMs) {
  entries.set(key, { value, expiresAt: ttlMs ? Date.now() + ttlMs : 0 });
  return value;
}
```

### cacheDelete - src/cache.js:25-27

```javascript
function cacheDelete(key) {
  return entries.delete(key);
}
```

### invalidatePrefix - src/cache.js:29-38

```javascript
function invalidatePrefix(prefix) {
  let removed = 0;
  for (const key of entries.keys()) {
    if (key.startsWith(prefix)) {
      entries.delete(key);
      removed++;
    }
  }
  return removed;
}
```

### parseHash - src/auth/password.js:12-18

```javascript
function parseHash(stored) {
  const [scheme, iterations, salt, hash] = String(stored).split('$');
  if (scheme !== 'pbkdf2' || !salt || !hash) {
    return null;
  }
  return { iterations: Number(iterations), salt, hash };
}
```

### withCache - src/cache.js:40-46

```javascript
function withCache(key, ttlMs, compute) {
  const hit = cacheGet(key);
  if (hit !== null) {
    return hit;
  }
  return cacheSet(key, compute(), ttlMs);
}
```

### currentUser - src/auth/middleware.js:12-15

```javascript
function currentUser(request) {
  const token = tokenFromCookie(request.headers && request.headers.cookie);
  return readSession(token);
}
```

### newToken - src/auth/session.js:8-10

```javascript
function newToken() {
  return crypto.randomBytes(24).toString('base64url');
}
```

### createSession - src/auth/session.js:16-24

```javascript
function createSession(user, ttlHours) {
  trace('auth.session.create', { user: user.id });
  recordChange(user.id, 'login', 'session');
  const token = newToken();
  const expiresAt = Date.now() + (ttlHours || DEFAULT_TTL_HOURS) * 3600 * 1000;
  const session = { token, userId: user.id, role: user.role || 'reader', expiresAt };
  cacheSet(sessionKey(token), session);
  return session;
}
```

### isExpired - src/auth/session.js:26-28

```javascript
function isExpired(session, now) {
  return !session || session.expiresAt <= (now || Date.now());
}
```

### readSession - src/auth/session.js:30-40

```javascript
function readSession(token) {
  if (!token) {
    return null;
  }
  const session = cacheGet(sessionKey(token));
  if (isExpired(session)) {
    cacheDelete(sessionKey(token));
    return null;
  }
  return session;
}
```

### revokeSession - src/auth/session.js:42-46

```javascript
function revokeSession(token) {
  trace('auth.session.revoke', {});
  recordChange('system', 'logout', 'session');
  cacheDelete(sessionKey(token));
}
```

### verifyPassword - src/auth/password.js:20-29

```javascript
function verifyPassword(password, stored) {
  const parsed = parseHash(stored);
  if (!parsed) {
    return false;
  }
  const candidate = crypto
    .pbkdf2Sync(password, parsed.salt, parsed.iterations, KEY_LENGTH, 'sha256')
    .toString('hex');
  return crypto.timingSafeEqual(Buffer.from(candidate), Buffer.from(parsed.hash));
}
```

### hashPassword - src/auth/password.js:6-10

```javascript
function hashPassword(password, salt) {
  const useSalt = salt || crypto.randomBytes(16).toString('hex');
  const hash = crypto.pbkdf2Sync(password, useSalt, ITERATIONS, KEY_LENGTH, 'sha256').toString('hex');
  return `pbkdf2$${ITERATIONS}$${useSalt}$${hash}`;
}
```

### needsRehash - src/auth/password.js:31-34

```javascript
function needsRehash(stored) {
  const parsed = parseHash(stored);
  return !parsed || parsed.iterations < ITERATIONS;
}
```

### isStrongPassword - src/auth/password.js:36-38

```javascript
function isStrongPassword(password) {
  return typeof password === 'string' && password.length >= 12 && /\d/.test(password) && /[A-Z]/.test(password);
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 62c29533 | 2026-09-07 | Priya Nair | bug fix | session ttl back to three days | 1 |
| 11fa5afd | 2026-08-08 | Priya Nair | feature | editor role between author and admin | 1 |
| d7d1a7c4 | 2026-07-27 | Priya Nair | security | raise pbkdf2 iterations | 1 |
| a8bdb0c9 | 2026-07-12 | Priya Nair | security | secure and samesite cookie flags | 1 |
| b49f71af | 2026-07-08 | Priya Nair | security | httponly session cookie | 1 |
| 36fe016c | 2026-04-05 | Priya Nair | test | password hashing | 1 |
| eacb5e3c | 2026-03-31 | Priya Nair | feature | sessions and role middleware | 3 |
| 75b2b339 | 2026-03-26 | Priya Nair | security | pbkdf2 password hashing | 1 |
