# Feature: admin

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | feature (1) |
| Scope | admin |
| Active | 2026-05-31 to 2026-05-31 |
| Commits | 1 |
| People | Ada Reyes (1) |
| Ask first | Ada Reyes <ada@example.com> |
| Files | 2 |
| Functions | 5 |
| Highest risk | topAuthors - 9 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-05-31 **feature**: daily report and csv export - Ada Reyes

## Where the code lives

### Files

- `prisma/schema.prisma` - 1 commit
- `src/admin/reports.js` - 1 commit

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `topAuthors` | src/admin/reports.js:6-12 | 9 (low) | 2 | 0 | no data |
| `dailyReport` | src/admin/reports.js:14-23 | 7 (low) | 1 | 5 | no data |
| `csvCell` | src/admin/reports.js:25-28 | 7 (low) | 1 | 0 | no data |
| `exportCsv` | src/admin/reports.js:30-33 | 7 (low) | 1 | 1 | no data |
| `weeklyReportV1` | src/admin/reports.js:36-39 | -4 (unused) | 0 | 2 | no data |

## How it fits together

### Calls inside the feature

- `dailyReport` -> `topAuthors`
- `exportCsv` -> `csvCell`
- `weeklyReportV1` -> `topAuthors`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `dailyReport` is called by `handleReport` (src/api.js:116-123)
- `exportCsv` is called by `handleReport` (src/api.js:116-123)

### What the feature depends on

- `trace` (src/telemetry.js:3-9) - used by `dailyReport`
- `listPosts` (src/store.js:43-45) - used by `dailyReport`, `weeklyReportV1`
- `popularTags` (src/tags.js:11-22) - used by `dailyReport`
- `recentChanges` (src/audit.js:16-18) - used by `dailyReport`

## Risks to keep in mind

- `topAuthors` - LOW (9): 2 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `dailyReport` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `csvCell` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `exportCsv` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `weeklyReportV1` - UNUSED (-4): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1

## Source

### topAuthors - src/admin/reports.js:6-12

```javascript
function topAuthors(posts, limit) {
  const counts = new Map();
  for (const post of posts) {
    counts.set(post.authorId, (counts.get(post.authorId) || 0) + 1);
  }
  return [...counts.entries()].sort((a, b) => b[1] - a[1]).slice(0, limit || 5);
}
```

### dailyReport - src/admin/reports.js:14-23

```javascript
function dailyReport(store) {
  trace('admin.report.daily', {});
  const posts = listPosts(store);
  return {
    posts: posts.length,
    authors: topAuthors(posts),
    tags: popularTags(posts, 5),
    changes: recentChanges(50).length,
  };
}
```

### csvCell - src/admin/reports.js:25-28

```javascript
function csvCell(value) {
  const text = String(value === undefined || value === null ? '' : value);
  return /[",\n]/.test(text) ? `"${text.replace(/"/g, '""')}"` : text;
}
```

### exportCsv - src/admin/reports.js:30-33

```javascript
function exportCsv(rows, columns) {
  const header = columns.map(csvCell).join(',');
  return [header, ...rows.map((row) => columns.map((c) => csvCell(row[c])).join(','))].join('\n');
}
```

### weeklyReportV1 - src/admin/reports.js:36-39

```javascript
function weeklyReportV1(store) {
  const posts = listPosts(store);
  return `posts=${posts.length};authors=${topAuthors(posts, 3).length}`;
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| ec04992e | 2026-05-31 | Ada Reyes | feature | daily report and csv export | 2 |
