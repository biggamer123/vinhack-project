# Feature: search

> Generated by Blast Radius on 2026-09-13 12:00 UTC from the call graph, git history and risk scores. Every statement below is measured, not inferred - but code changes after this was generated are not reflected, so check the source before relying on line numbers.

## At a glance

| | |
|---|---|
| Type | refactor (1), feature (1) |
| Scope | search |
| Active | 2026-03-14 to 2026-05-20 |
| Commits | 2 |
| People | Jonah Lee (2) |
| Ask first | Jonah Lee <jonah@example.com> |
| Files | 1 |
| Functions | 5 |
| Highest risk | tokenize - 11 (low) |

## What this feature is

Its commits, oldest first. These are the only description of intent Blast Radius has - they are the authors' own words.

- 2026-03-14 **feature**: title search with ranking - Jonah Lee
- 2026-05-20 **refactor**: weight title matches higher - Jonah Lee

## Where the code lives

### Files

- `src/search.js` - 2 commits

### Functions

| Function | Location | Risk | Callers | Calls | Coverage |
|---|---|---|---|---|---|
| `tokenize` | src/search.js:5-7 | 11 (low) | 3 | 1 | no data |
| `scoreMatch` | src/search.js:22-30 | 7 (low) | 1 | 1 | no data |
| `rankResults` | src/search.js:32-38 | 7 (low) | 1 | 3 | no data |
| `search` | src/search.js:40-43 | 7 (low) | 1 | 4 | no data |
| `buildIndex` | src/search.js:9-20 | -12 (unused) | 0 | 3 | no data |

## How it fits together

### Calls inside the feature

- `scoreMatch` -> `tokenize`
- `rankResults` -> `tokenize`
- `rankResults` -> `scoreMatch`
- `search` -> `rankResults`
- `buildIndex` -> `tokenize`

### Called from outside the feature

Code that is not part of this feature but calls into it. Changing the feature can break these - check them before changing a signature or behaviour.

- `search` is called by `handleSearch` (src/api.js:54-58)

### What the feature depends on

- `slugify` (src/format.js:1-3) - used by `tokenize`
- `query` (src/db/pool.js:9-17) - used by `rankResults`, `search`
- `trace` (src/telemetry.js:3-9) - used by `search`, `buildIndex`
- `listPosts` (src/store.js:43-45) - used by `search`, `buildIndex`

## Risks to keep in mind

- `tokenize` - LOW (11): 3 callers, coverage no data, 0 changes in 90 days, bus factor 1
- `scoreMatch` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `rankResults` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `search` - LOW (7): 1 caller, coverage no data, 0 changes in 90 days, bus factor 1
- `buildIndex` - UNUSED (-12): 0 callers, coverage no data, 0 changes in 90 days, bus factor 1

## Source

### tokenize - src/search.js:5-7

```javascript
function tokenize(text) {
  return slugify(text).split('-').filter(Boolean);
}
```

### scoreMatch - src/search.js:22-30

```javascript
function scoreMatch(post, tokens) {
  let score = 0;
  for (const token of tokenize(post.title)) {
    if (tokens.includes(token)) {
      score += 3;
    }
  }
  return score;
}
```

### rankResults - src/search.js:32-38

```javascript
function rankResults(posts, query) {
  const tokens = tokenize(query);
  return posts
    .map((post) => ({ post, score: scoreMatch(post, tokens) }))
    .filter((hit) => hit.score > 0)
    .sort((a, b) => b.score - a.score);
}
```

### search - src/search.js:40-43

```javascript
function search(store, query) {
  trace('search.run', { query });
  return rankResults(listPosts(store), query);
}
```

### buildIndex - src/search.js:9-20

```javascript
function buildIndex(store) {
  trace('search.index', {});
  const index = new Map();
  for (const post of listPosts(store)) {
    for (const token of tokenize(post.title + ' ' + post.body)) {
      const bucket = index.get(token) || [];
      bucket.push(post.id);
      index.set(token, bucket);
    }
  }
  return index;
}
```

## History

| Commit | Date | Author | Type | Message | Files |
|---|---|---|---|---|---|
| 1b5aa5da | 2026-05-20 | Jonah Lee | refactor | weight title matches higher | 1 |
| 316ff147 | 2026-03-14 | Jonah Lee | feature | title search with ranking | 1 |
